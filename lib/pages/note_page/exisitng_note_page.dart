import 'package:flutter/material.dart';

class ExistingNoteScreen extends StatelessWidget {
  const ExistingNoteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
